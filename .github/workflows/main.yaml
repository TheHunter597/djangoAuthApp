name: django-auth
on: push

jobs:
  # test:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v2
  #     - name: test the application
  #       run: |
  #         docker build -t django-auth-test -f Dockerfile.test .
  #         docker run django-auth-test
  build:
    runs-on: ubuntu-latest
    continue-on-error: true
    outputs:
      version: ${{ steps.get_required_version.outputs.version }}
    steps:
      - uses: actions/checkout@v2
      - name: get required version
        id: get_required_version
        run: |
          version=$(bash djangoAuthApp/scripts/get-version.sh "${{ github.event.head_commit.message }}")
          echo "version=$version" >> "$GITHUB_OUTPUT"
          echo "version=$version" >> "$GITHUB_ENV"

      - name: update git repo
        continue-on-error: true
        env:
          GH_PAT: ${{ secrets.GH_PAT }}
        run: |
          git config --global user.email "thehunter597777@gmail.com"
          git config --global user.name "TheHunter597"
          git remote set-url origin https://x-access-token:${GH_PAT}@github.com/TheHunter597/djangoAuthApp.git
          git add .
          git commit -m "Bump version to ${{ steps.get_required_version.outputs.version }} [skip ci]"
          git push
      - name: update git repo 2
        continue-on-error: true
        env:
          GH_PAT: ${{ secrets.GH_PAT }}
        run: |
          git config --global user.email "thehunter597777@gmail.com"
          git config --global user.name "TheHunter597"
          git remote set-url origin https://TheHunter597:${GH_PAT}@github.com/TheHunter597/djangoAuthApp.git
          git push
      - name: update git repo 3
        continue-on-error: true
        env:
          GH_PAT: ${{ secrets.GH_PAT }}
        run: |
          git config --global user.email "thehunter597777@gmail.com"
          git config --global user.name "TheHunter597"
          git remote set-url origin https://x-access-token:$GH_PAT@github.com/TheHunter597/djangoAuthApp.git

          git push
      - name: update git repo 4
        continue-on-error: true
        env:
          GH_PAT: ${{ secrets.GH_PAT }}
        run: |
          git config --global user.email "thehunter597777@gmail.com"
          git config --global user.name "TheHunter597"
          git remote set-url origin https://x-access-token:${{secrets.GH_PAT}}@github.com/TheHunter597/djangoAuthApp.git

          git push
      - name: update git repo 5
        continue-on-error: true
        env:
          GH_PAT: ${{ secrets.GH_PAT }}
        run: |
          git config --global user.email "thehunter597777@gmail.com"
          git config --global user.name "TheHunter597"
          git remote set-url origin https://TheHunter597:$GH_PAT@github.com/TheHunter597/djangoAuthApp.git
          git push
      - name: update git repo 6
        continue-on-error: true
        env:
          GH_PAT: ${{ secrets.GH_PAT }}
        run: |
          git config --global user.email "thehunter597777@gmail.com"
          git config --global user.name "TheHunter597"
          git remote set-url origin https://TheHunter597:${{secrets.GH_PAT}}@github.com/TheHunter597/djangoAuthApp.git
          git push
      - name: update git repo 7
        continue-on-error: true
        env:
          GH_PAT: ${{ secrets.GH_PAT }}
        run: |
          git config --global user.email "thehunter597777@gmail.com"
          git config --global user.name "TheHunter597"
          git remote set-url origin https://TheHunter597:${{secrets.PASSWORD}}@github.com/TheHunter597/djangoAuthApp.git
          git push
      # - name: build docker image
      #   run: |
      #     docker build -t ecommercemainreg.azurecr.io/auth:${{ env.version }} -f Dockerfile .
      # - uses: azure/login@v2
      #   with:
      #     creds: ${{ secrets.AZURE_CREDENTIALS }}
      # - name: adding containers registry
      #   run: |
      #     az acr login --name ecommercemainreg
      #     docker push ecommercemainreg.azurecr.io/auth:${{ env.version }}
